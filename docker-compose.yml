services:
  mongo:
    image: mongo:7
    container_name: hrms-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: hrms_portal_dev
    volumes:
      - hrms_mongo_data:/data/db

volumes:
  hrms_mongo_data:
